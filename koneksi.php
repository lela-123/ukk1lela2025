<?php
$servername = "";
$database = "";
$username = "root";
$password = "";

$conn = ($servername, $username, $password, $database);

if (!$conn) {
    die("Koneksi Gagal : " .())
} else {
    echo "Koneksi Berhasil";
}
